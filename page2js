new TypeIt(".simpleUsage", {
    strings: ["Grace Hong"],
    speed: 50,
    waitUntilVisible: true,
  })
  	.break({ delay: 500 })
    .break({ delay: 500 })
    .type("My favorite film is Ponyo! &hearts;")
    .pause(300)
    
    .go();
